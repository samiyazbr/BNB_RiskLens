# Next.js env vars (copy to .env.local)
# Public URLs should be prefixed with NEXT_PUBLIC_
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: if the website ever proxies AI calls server-side
OPENAI_API_KEY=your_server_side_openai_key
